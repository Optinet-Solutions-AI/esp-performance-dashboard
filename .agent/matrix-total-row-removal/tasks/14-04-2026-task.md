# Task: Remove Redundant ESP Total Rows — Matrix View

**Date:** 14-04-2026
**Status:** Completed
**File:** `src/components/views/MatrixView.tsx`

## What Was Done

Removed two redundant `{espName} — Total` rows from the ESP Deliverability Matrix table:

1. **Collapsed-state total row** (was lines 318–328)
   - Rendered when an ESP row was collapsed (`!espEx`)
   - Showed the same data already visible in the ESP header row above it
   - Replaced with `if (!espEx) return` — collapse now simply stops rendering without adding a duplicate row

2. **Expanded-state grand total row** (was lines 463–472)
   - Rendered at the bottom of an expanded ESP section
   - Same aggregated totals already shown in the ESP header row
   - Removed entirely

## What Was NOT Changed

- IP-level total rows (`{ip} — total`) — kept, serve a purpose when drilling into from-domain breakdown
- From-domain total rows (`{fd} — total`) — kept, serve a purpose when drilling into provider breakdown
- `DataRow` component and `isTotal` / `isFdTotal` props — kept (still used by other total rows)
- No data paths, parsers, merge logic, or store state touched

## Risk

Low. Pure display change. No data integrity impact.
