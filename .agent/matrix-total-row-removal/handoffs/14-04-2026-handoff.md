# Handoff: Matrix Total Row Removal

**Date:** 14-04-2026
**Status:** Complete — no follow-up required

## Summary

Removed the `{espName} — Total` rows from the ESP Deliverability Matrix view. These rows duplicated the data already shown in each ESP's header row, making the table visually noisy.

## Change Location

`src/components/views/MatrixView.tsx`

- Collapsed-state block: `if (!espEx) { rows.push(...total row...) }` → simplified to `if (!espEx) return`
- Expanded-state block: `// ESP grand total` block removed entirely

## Behaviour Before vs After

| State | Before | After |
|-------|--------|-------|
| ESP collapsed | Header row + Total row (same data) | Header row only |
| ESP expanded | Header row + IPs + From-domains + Grand total row | Header row + IPs + From-domains (no grand total) |

## Notes for Next Agent

- `isTotal` prop on `DataRow` is still present and used by any future total rows — do not remove it
- IP and from-domain totals are untouched
- No Supabase, store, or parser changes — safe to ship immediately
